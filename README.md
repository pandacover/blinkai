# blinkai

Short film generator webapp (TypeScript, Bun, OpenRouter).

## Planning

This repo uses [Matt Pocock's engineering skills](https://github.com/mattpocock/skills), vendored under `.agents/skills/`.

The product route is being charted with **wayfinder** (decision map under `.scratch/`, once destination grilling finishes). Wayfinder plans; it does not build. After the map clears: `to-spec` → `to-tickets` → `implement`.

## Agent setup

See `AGENTS.md` and `docs/agents/` for issue tracker, triage labels, and domain-doc layout.
