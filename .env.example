# OpenRouter (required). Bun server reads this; never expose to the browser.
# Create a key at https://openrouter.ai/keys — put the real value in gitignored `.env`.
OPENROUTER_API_KEY=

# Optional: override local Project storage root (default: <app-root>/data).
# BLINKAI_DATA_DIR=
