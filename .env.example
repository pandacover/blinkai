# OpenRouter (required) — https://openrouter.ai/keys
# Bun server only; never expose to the browser.
OPENROUTER_API_KEY=

# Optional — default is <app-root>/data (see wayfinder ticket 04)
# BLINKAI_DATA_DIR=
